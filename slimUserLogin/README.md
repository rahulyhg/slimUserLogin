# Slim User Login

Slim 3 skeleton application was used to build this demo. 
We are also using the PHP-View template renderer. 

## Install the Application
Outside from the Docker setup, the only thing you need to do is to run composer install:

	php composer.phar install

